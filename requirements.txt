bittensor
torch==2.7.1
requests==2.32.5
aiohttp==3.13.3
# timelock is NOT installed from PyPI.
# Live miner W_time is 356 bytes (ark-serialize 0.5). PyPI
# `timelock==0.0.1.dev0` + `timelock-wasm-wrapper==0.0.2` is the old
# 372-byte stack and cannot decrypt current subnet payloads.
# install_reqs.sh builds timelock 0.0.2.dev0 + wasm wrapper 0.3.0 from
# https://github.com/ideal-lab5/timelock @ ccccca019409c89f31fd687352db8060bfb4aae6
# Miners: see MINER_GUIDE.md (do not `pip install timelock`).
python-dotenv==1.2.2
boto3==1.42.70
numpy==2.2.6
scikit-learn==1.7.2
scipy==1.15.3
matplotlib==3.10.8
pandas==2.3.3
seaborn==0.13.2
joblib==1.5.3
aiobotocore==3.3.0
shap==0.49.1
yfinance==1.2.0
tqdm==4.67.3
xgboost==3.2.0
threadpoolctl==3.6.0
cryptography==46.0.5
flask>=3.0
gunicorn>=22.0
# FLOW collateral reads (validator) + settlement daemon posting (team)
web3>=7.6,<8
# pm2 (installed globally via npm)
